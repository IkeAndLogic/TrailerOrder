@model List<TrailerOrder.Models.DriverTractorAssignmentHistory>

@if (Model.Count == 0)
{
    <p> Nothing to Show yet!</p>
}
else
{

    <div class="container">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        Driver Names:
                    </th>
                </tr>
            </thead>
            <tbody>

                <tr>
                    <td>
                        Tractors: 
                </tr>


            <tbody>
        </table>
    </div>
}